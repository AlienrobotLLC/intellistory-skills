# evals

Golden prompts per skill: a prompt an agent might receive, and what a good run does (which
commands / tools it reaches for, in what order, and what it must NOT do). One file per skill,
added alongside the skill.
